# Publishing

See [PUBLISHING.md](https://github.com/PyDevices/palettes/blob/main/PUBLISHING.md) in the repository root.

Releases are tag-driven (`vX.Y.Z`). CI syncs sources into `PyDevices/micropython-lib`, uploads TestPyPI wheels, and rebuilds the MIP index on `gh-pages`.
